# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/*
# ...pero el script del instalador SÍ se versiona (se perdió una vez por estar ignorado).
# Se ignora el contenido de build/ (no el dir) para poder re-incluir este archivo.
!build/installer.nsh

# Build artifacts (instaladores y output compilado de Electron)
dist/
electron-dist/

# Scripts descartables de prueba
electron-test3.mjs

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.publish

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
